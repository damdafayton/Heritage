import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  global: {
    marginTop: 16,
  },
  text: {
    marginTop: 4,
  },
  textDataView: {padding: 10, borderRadius: 4},
});

export const globalStyles = styles;
